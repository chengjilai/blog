[project]
name = "blog"
version = "0.1.0"
description = "fetch blogs"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "lxml>=6.1.1",
]

[dependency-groups]
dev = [
    "coverage>=7.14.0",
    "ruff>=0.15.14",
    "ty>=0.0.38",
]
